# Template: do NOT commit real secrets.
# Create the actual secret with:
#
#   kubectl create secret generic sova-secrets -n sova \
#     --from-literal=BOT_TOKEN=<your-bot-token> \
#     --from-literal=ANTHROPIC_API_KEY=<your-api-key> \
#     --from-literal=ENCRYPTION_KEY=<64-hex-char-key> \
#     --from-literal=POSTGRES_PASSWORD=<db-password> \
#     --from-literal=ZENMONEY_CONSUMER_KEY=<zm-key> \
#     --from-literal=ZENMONEY_CONSUMER_SECRET=<zm-secret>
#
# Or use SealedSecrets / External Secrets Operator in production.
apiVersion: v1
kind: Secret
metadata:
  name: sova-secrets
  namespace: sova
  labels:
    app.kubernetes.io/part-of: sova
type: Opaque
data:
  # base64-encoded placeholders — replace with real values
  BOT_TOKEN: CHANGEME
  ANTHROPIC_API_KEY: CHANGEME
  ENCRYPTION_KEY: CHANGEME
  POSTGRES_PASSWORD: CHANGEME
  ZENMONEY_CONSUMER_KEY: CHANGEME
  ZENMONEY_CONSUMER_SECRET: CHANGEME
