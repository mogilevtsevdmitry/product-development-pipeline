apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: sova
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: sova
spec:
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP
  clusterIP: None
