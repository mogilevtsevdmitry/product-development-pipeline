# Сова (Sova) — AI Финансовый Помощник

**Дата:** 2026-04-08
**Статус:** Утверждён

## 1. Обзор продукта

**Сова** — AI-финансовый помощник с Telegram-ботом как основным интерфейсом и веб-дашбордом для глубокой аналитики. Продукт объединяет учёт финансов, управление инвестициями и персонализированные AI-рекомендации в одном месте.

### Ключевое отличие

Большинство финансовых приложений отлично логируют транзакции, но не говорят пользователю, что ДЕЛАТЬ с данными. Сова — это не трекер, а **советник**: анализирует, объясняет, рекомендует, торгует.

### Целевая аудитория

Русскоязычные пользователи с любым уровнем финансовой грамотности — от новичков до активных инвесторов. AI адаптирует стиль общения под уровень пользователя.

### Бизнес-модель

Freemium с pay-per-use для AI-функций:
- **Бесплатно:** учёт финансов, интеграции (ZenMoney, T-Bank), автокатегоризация, базовые запросы, уведомления
- **Платно (AI-баланс):** глубокая аналитика, инвестиционные рекомендации, персонализированные дайджесты, свободный AI-чат, торговые рекомендации

Оплата через Telegram Stars. Пользователь пополняет баланс (от 100₽), каждый AI-запрос списывает фиксированную сумму. Баланс бессрочный. Вывод средств возможен с уменьшением баланса.

## 2. Архитектура

### Общая схема

```
┌─────────────────────────────────────────────────┐
│                   Пользователь                   │
│         Telegram  ←──────→  Web Dashboard        │
└──────┬──────────────────────────────┬────────────┘
       │                              │
┌──────▼──────────────────────────────▼────────────┐
│              ОСНОВНОЙ СЕРВИС (FastAPI)            │
│                                                   │
│  ┌───────────┐ ┌───────────┐ ┌────────────────┐  │
│  │ Telegram  │ │  REST API │ │  Бизнес-логика │  │
│  │ Bot       │ │ (дашборд) │ │  (транзакции,  │  │
│  │ (aiogram) │ │           │ │  цели, баланс) │  │
│  └───────────┘ └───────────┘ └────────────────┘  │
│  ┌───────────┐ ┌───────────┐ ┌────────────────┐  │
│  │  Auth &   │ │ Billing   │ │  Integrations  │  │
│  │  Users    │ │ (кошелёк) │ │  (ZM, T-Bank)  │  │
│  └───────────┘ └───────────┘ └────────────────┘  │
└──────────────────────┬───────────────────────────┘
                       │ Redis (очередь задач)
          ┌────────────┼────────────┐
          ▼            ▼            ▼
   ┌────────────┐ ┌─────────┐ ┌─────────────┐
   │ AI Worker  │ │  Cron   │ │  Trade      │
   │            │ │ Worker  │ │  Worker     │
   │ • Инсайты  │ │ • Синк  │ │ • Ордера   │
   │ • Рекомен- │ │   ZM/TB │ │ • Подтверж-│
   │   дации    │ │ • Новос-│ │   дение    │
   │ • Дайджест │ │   ти    │ │             │
   │ • Чат      │ │ • Курсы │ │             │
   └────────────┘ └─────────┘ └─────────────┘
                       │
                ┌──────▼──────┐
                │ PostgreSQL  │
                │ + Redis     │
                └─────────────┘
```

### Компоненты

- **Основной сервис (FastAPI + aiogram 3)** — обрабатывает webhook бота, REST API для дашборда, CRUD операции, авторизация, биллинг. Синхронные операции.
- **AI Worker** — отдельный процесс, забирает задачи из Redis. Все LLM-вызовы (Claude API). Изолирован, т.к. запросы к LLM занимают 2-15 секунд.
- **Cron Worker** — периодические задачи: синхронизация ZenMoney (каждые 4 часа), обновление портфеля T-Bank (каждый час 7:00-23:00), парсинг новостей (каждые 2 часа), курсы валют, генерация дайджестов.
- **Trade Worker** — исполнение торговых операций через T-Bank gRPC API. Подтверждение → исполнение → проверка статуса → уведомление.

### Стек технологий

| Компонент | Технология |
|-----------|-----------|
| Бэкенд | Python 3.12, FastAPI, aiogram 3 |
| ORM | SQLAlchemy 2.0 + Alembic |
| Очередь задач | arq (async Redis queue) |
| База данных | PostgreSQL 16 |
| Кеш/очередь | Redis 7 |
| Фронтенд | React 18 + TypeScript |
| AI | Claude API (Sonnet + Haiku) |
| T-Bank | tinkoff-investments (gRPC SDK) |
| Локальная разработка | Docker Compose |
| Прод | Kubernetes + ArgoCD |

## 3. Telegram-бот

### Онбординг

1. `/start` → приветствие от Совы (3 предложения)
2. Выбор интеграций: "Подключить ZenMoney" / "Подключить T-Bank" / "Начать без интеграций"
3. Для каждой интеграции: OAuth-ссылка + выбор периода импорта ("За последний месяц" / "3 месяца" / "Всё время" / "Свой период")
4. Выбор уровня: "Я новичок" / "Уже веду бюджет" / "Инвестирую активно"
5. Готово — первый дайджест через 24 часа после синхронизации

### Быстрый ввод расходов

Бот понимает естественный язык:
- `кофе 350` → категория "Еда", 350₽
- `такси 600 работа` → категория "Транспорт", 600₽, тег "работа"
- Фото чека → распознавание через Claude Vision API + автокатегоризация
- Неочевидная категория → бот спрашивает с inline-кнопками

### Меню (inline-кнопки)

```
/menu или "Меню"
├── 💰 Баланс          — текущие остатки по счетам
├── 📊 Сегодня          — траты за сегодня + сравнение с обычным днём
├── 📈 Портфель         — стоимость, P&L, топ-3 позиции
├── 🎯 Цели             — прогресс по активным целям
├── 🔔 Настройки        — уведомления, интеграции, уровень
├── 💎 Баланс AI        — остаток, пополнение, вывод
└── 📖 Помощь           — что умеет бот
```

### AI-команды (платные)

**ВАЖНО: AI предоставляет аналитику и образовательный контент, но НЕ даёт директивных рекомендаций "купи/продай" (ст. 6.1 N 39-ФЗ). Торговля полностью отделена от AI-ответов.**

- "Что с моими финансами?" → аналитический обзор текущего состояния (5₽)
- "Расскажи про мой портфель" → анализ диверсификации, P/E, дивидендная доходность, сравнение с индексами (8₽)
- "Расскажи про {тикер}" → аналитическая справка: фундаментальные показатели, динамика, сектор, доля в портфеле (10₽)
- "Куда уходят деньги?" → паттерны трат, аномалии, сравнение с прошлыми периодами (5₽)
- "Реально ли накопить 500к к декабрю?" → моделирование сценариев на основе текущих данных (5₽)
- "Дайджест" → принудительная генерация финансовой сводки (12₽)
- Свободный AI-чат (3₽ за сообщение)

**Запрещённые формулировки AI:** "купи", "продай", "стоит купить", "рекомендую", "советую приобрести". AI отвечает фактами и аналитикой, решение принимает пользователь.

Перед каждым платным запросом бот показывает стоимость и запрашивает подтверждение.

### Торговля

```
Пользователь: купить SBER 10 лотов
Бот: 📋 Заявка на покупку:
     SBER (Сбербанк) × 10 лотов
     Цена: ~2,850₽ за лот → ~28,500₽
     ⚠️ Сделка инициирована вами. Сервис не даёт инвестиционных рекомендаций.
     [Подтвердить] [Отмена]
→ [Подтвердить]
Бот: ✅ Заявка исполнена: SBER × 10 по 2,847₽
     Комиссия: 8.54₽
```

Двойное подтверждение. Лимит на сделку без дополнительного подтверждения — 50,000₽ (настраивается). Превышение → дополнительный запрос.

**Механика исполнения:**
- Подтверждение действует 60 секунд. После таймаута → "Заявка отменена, цена могла измениться"
- Если цена изменилась более чем на 1% между показом и подтверждением → повторный показ актуальной цены
- Заявки по умолчанию рыночные. Лимитные заявки — через явный синтаксис: "купить SBER 10 лотов по 2800"
- Rate limit на торговлю: не более 10 заявок в минуту (T-Bank API ограничение)

### Уведомления (настраиваемые)

| Триггер | Пример | По умолчанию |
|---------|--------|-------------|
| Крупная трата | "Трата 15,000₽ в Ашан — в 3 раза больше обычного" | Вкл |
| Приближение к лимиту | "Еда: 80% бюджета, осталось 4,200₽ на 8 дней" | Вкл |
| Просадка портфеля | "Портфель -3.2% за день (-12,400₽)" | Вкл, порог 3% |
| Дивиденды/купоны | "Начислены дивиденды SBER: 1,200₽" | Вкл |
| Утренний дайджест | Краткая сводка: баланс, курсы, план | Выкл |
| Еженедельный отчёт | Полный разбор недели с AI-инсайтами | Вкл (вс 10:00) |
| Персонализированная новость | "Газпром -4% — у тебя 15% портфеля в GAZP" | Вкл |

### Тон общения

Бот говорит от лица Совы — спокойно, мудро, без панибратства:
- "🦉 Доброе утро! Вот что произошло за ночь..."
- "🦉 Заметила крупную трату — всё ок?"
- "🦉 Портфель вырос на 2.3% за неделю, молодец"

## 4. Веб-дашборд

### Авторизация

Вход через Telegram Login Widget — без паролей. Тот же аккаунт что в боте. Доступ из бота (кнопка "📊 Дашборд") или напрямую по URL.

### Страницы

**Главная (обзор):**
- Общий баланс по всем счетам
- Мини-график доходов/расходов за 30 дней
- Прогресс по целям (прогресс-бары)
- Стоимость портфеля + P&L за день
- Последние 5 транзакций
- Виджет "AI-инсайт дня" (бесплатный)

**Транзакции:**
- Таблица с фильтрами: период, категория, счёт, сумма, поиск
- Группировка: по дням / неделям / месяцам
- Массовое редактирование категорий
- Экспорт в CSV

**Аналитика:**
- Расходы по категориям (donut chart + тренд по месяцам)
- Доходы vs расходы (bar chart)
- Топ категорий с динамикой
- Сравнение месяц к месяцу, аномалии
- Кешфлоу heatmap по дням месяца

**Портфель:**
- Состав (pie chart по секторам, валютам, типам)
- Таблица позиций: тикер, кол-во, средняя/текущая цена, P&L, доля
- График стоимости портфеля во времени
- Дивидендный календарь
- История операций

**Цели:**
- Список с прогресс-барами
- Создание/редактирование: название, сумма, дедлайн, счёт
- Прогноз достижения на основе текущего темпа
- AI-рекомендация по ускорению (платно)

**Новости и рынок:**
- Персонализированная лента (AI-фильтрация под портфель)
- Курсы валют (USD, EUR, CNY)
- Индексы (MOEX, S&P 500)
- Каждая новость с пометкой: как влияет на портфель

**Настройки:**
- Интеграции: подключение/отключение, управление токенами
- Уведомления: триггеры и расписание
- Профиль: уровень, валюта отображения
- AI-баланс: история, пополнение, вывод
- Категории: управление пользовательскими категориями

### Адаптивность

Mobile-first (PWA). На телефоне дашборд работает как приложение. Основное мобильное взаимодействие — через бот, дашборд для глубокого анализа.

## 5. Интеграции

### ZenMoney

- **Подключение:** OAuth 2.0 (consumer_key + consumer_secret)
- **Токены:** access_token + refresh_token хранятся зашифрованно. При истечении access_token — автоматическое обновление через refresh_token. Если refresh_token невалиден — уведомление пользователю "🦉 Подключение к ZenMoney разорвано, переподключите" + статус интеграции → "disconnected"
- **Синхронизация:** diff-based API. Первый импорт — полный за выбранный период. Далее каждые 4 часа — дельта с последнего timestamp
- **Ошибки синхронизации:** при таймауте или ошибке API — 3 попытки с экспоненциальным backoff (30с, 2м, 10м). После 3 неудач — статус "error", уведомление пользователю, следующая попытка через 1 час
- **Данные:** транзакции (сумма, дата, описание, категория, счёт), счета (баланс, валюта), категории
- **Маппинг категорий:** AI автоматически маппит при первом импорте. Пользователь может исправить, бот запоминает
- **Дедупликация:** если ручная трата совпадает с импортом из ZM (сумма + дата ± 1 день) — запрос подтверждения

### T-Bank Invest

- **Подключение:** API-токен (пользователь создаёт в приложении T-Bank)
- **Протокол:** gRPC (tinkoff-investments Python SDK)
- **Чтение (cron, каждый час 7:00-23:00):**
  - Портфель: позиции, средние цены, текущие цены
  - Операции: покупки, продажи, дивиденды, купоны, комиссии
  - Статистика: P&L по позициям и портфелю
- **Торговля (Trade Worker):**
  - Рыночные и лимитные заявки
  - Двойное подтверждение в боте
  - Статус: pending → executed/cancelled → уведомление
  - Настраиваемый лимит на сделку (по умолчанию 50,000₽)
- **Sandbox:** для тестирования торговых операций
- **Ошибки синхронизации:** аналогично ZenMoney — 3 попытки с backoff, уведомление при неудаче

### Парсинг новостей

**Источники:**
- RSS/API: РБК, Коммерсант, Investing.com
- MOEX API: индексы и инструменты
- ЦБ РФ API: курсы валют, ключевая ставка
- Telegram-каналы (опционально, Telethon)

**Процесс:**
1. Cron Worker собирает сырые новости каждые 2 часа → Redis
2. AI Worker обрабатывает пачку каждые 4 часа: фильтрация, извлечение фактов, оценка влияния
3. При генерации дайджеста/алерта — AI сопоставляет с портфелем пользователя

## 6. AI-система

### Модели

| Операция | Модель | Себестоимость | Цена юзера |
|----------|--------|---------------|------------|
| Категоризация | Haiku | ~0.3₽ | Бесплатно |
| Быстрый вопрос | Haiku | ~0.5₽ | Бесплатно |
| Анализ расходов | Sonnet | ~3₽ | 5₽ |
| Анализ портфеля | Sonnet | ~5₽ | 8₽ |
| Аналитика по тикеру | Sonnet | ~5₽ | 10₽ |
| Полный дайджест | Sonnet | ~8₽ | 12₽ |
| AI-чат (за сообщение) | Sonnet | ~2₽ | 3₽ |

### Контекст AI-запроса

Каждый запрос получает релевантный контекст:

```
System prompt (роль Совы, уровень пользователя, дисклеймер)
+
Финансовый профиль:
  - доходы/расходы за 3 мес (агрегированные по категориям)
  - текущие балансы
  - активные цели и прогресс
+
Портфель (если релевантно):
  - позиции, P&L, распределение
  - последние операции
+
Релевантные новости (из news_cache)
+
Запрос пользователя
```

Контекст адаптируется под уровень пользователя:
- **Новичок:** простой язык, объяснение терминов, базовые концепции
- **Продвинутый:** цифры, коэффициенты, сравнение с бенчмарками

### Юридические ограничения AI (ст. 6.1 N 39-ФЗ)

AI system prompt содержит жёсткий запрет на:
- Директивные рекомендации: "купи", "продай", "стоит купить", "рекомендую"
- Персонализированные советы по конкретным сделкам
- Прогнозы цен с указанием направления ("вырастет", "упадёт")

AI может:
- Приводить фактические данные (P/E, дивидендная доходность, динамика)
- Сравнивать с индексами и бенчмарками
- Анализировать структуру портфеля (диверсификация, валютный баланс)
- Описывать риски ("высокая концентрация в одном секторе")
- Моделировать сценарии ("если сохранить текущий темп расходов...")

Торговые команды в боте **полностью отделены** от AI — пользователь сам инициирует сделки, бот лишь проксирует заявку в T-Bank API.

### Дайджесты

**Ежедневный (утренний, если включён):**
- Баланс, изменение за день
- Курсы валют
- Портфель P&L, значимые движения
- 1 совет или факт

**Еженедельный (воскресенье 10:00 по часовому поясу пользователя):**
- Доходы vs расходы, сравнение с предыдущей неделей
- Топ-3 категории расходов с динамикой
- Прогресс по целям
- Портфель P&L, дивиденды
- 2-3 персонализированных инсайта
- Релевантные новости

**Формат:** структурированный текст с эмодзи в Telegram.

## 7. Модель данных

### Ключевые сущности

```sql
-- Пользователи
users (
    telegram_id BIGINT PRIMARY KEY,
    username VARCHAR,
    first_name VARCHAR,
    level VARCHAR DEFAULT 'beginner',  -- beginner | intermediate | advanced
    ai_balance DECIMAL(10,2) DEFAULT 0,
    ai_balance_version INTEGER DEFAULT 0,  -- optimistic locking для конкурентных списаний
    timezone VARCHAR DEFAULT 'Europe/Moscow',  -- часовой пояс пользователя для дайджестов
    notification_settings JSONB DEFAULT '{}',
    created_at TIMESTAMPTZ DEFAULT NOW()
)

-- Интеграции
integrations (
    id UUID PRIMARY KEY,
    user_id BIGINT REFERENCES users,
    type VARCHAR NOT NULL,  -- zenmoney | tbank_invest
    access_token_encrypted BYTEA,
    refresh_token_encrypted BYTEA,  -- для ZenMoney OAuth
    token_expires_at TIMESTAMPTZ,
    sync_from_date DATE,
    last_synced_at TIMESTAMPTZ,
    last_error TEXT,  -- последняя ошибка синхронизации
    error_count INTEGER DEFAULT 0,  -- счётчик последовательных ошибок
    status VARCHAR DEFAULT 'active'  -- active | error | disconnected
)

-- Счета
accounts (
    id UUID PRIMARY KEY,
    user_id BIGINT REFERENCES users,
    name VARCHAR NOT NULL,
    currency VARCHAR(3) DEFAULT 'RUB',
    balance DECIMAL(12,2),
    source VARCHAR NOT NULL,  -- zenmoney | manual
    external_id VARCHAR,
    UNIQUE(user_id, source, external_id)
)

-- Категории
categories (
    id UUID PRIMARY KEY,
    user_id BIGINT REFERENCES users,  -- NULL = системная
    name VARCHAR NOT NULL,
    icon VARCHAR,
    parent_id UUID REFERENCES categories,
    type VARCHAR NOT NULL  -- expense | income | transfer
)

-- Транзакции
transactions (
    id UUID PRIMARY KEY,
    user_id BIGINT REFERENCES users,
    account_id UUID REFERENCES accounts,
    category_id UUID REFERENCES categories,
    amount DECIMAL(12,2) NOT NULL,
    currency VARCHAR(3) DEFAULT 'RUB',
    date DATE NOT NULL,
    description TEXT,
    source VARCHAR NOT NULL,  -- zenmoney | tbank | manual | ocr
    external_id VARCHAR,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(user_id, source, external_id)
)

-- Позиции портфеля
portfolio_positions (
    id UUID PRIMARY KEY,
    user_id BIGINT REFERENCES users,
    ticker VARCHAR NOT NULL,
    figi VARCHAR,
    name VARCHAR,
    quantity DECIMAL(12,6),  -- дробные лоты для иностранных акций и ETF
    avg_price DECIMAL(12,4),
    current_price DECIMAL(12,4),
    sector VARCHAR,
    asset_type VARCHAR,  -- stock | bond | etf | currency
    currency VARCHAR(3),
    updated_at TIMESTAMPTZ
)

-- Операции портфеля
portfolio_operations (
    id UUID PRIMARY KEY,
    user_id BIGINT REFERENCES users,
    ticker VARCHAR NOT NULL,
    operation_type VARCHAR NOT NULL,  -- buy | sell | dividend | coupon | commission
    quantity DECIMAL(12,6),
    price DECIMAL(12,4),
    total DECIMAL(12,2),
    executed_at TIMESTAMPTZ
)

-- Цели
goals (
    id UUID PRIMARY KEY,
    user_id BIGINT REFERENCES users,
    name VARCHAR NOT NULL,
    target_amount DECIMAL(12,2) NOT NULL,
    current_amount DECIMAL(12,2) DEFAULT 0,
    deadline DATE,
    status VARCHAR DEFAULT 'active',  -- active | achieved | abandoned
    ai_recommendation TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
)

-- Платёжные транзакции (пополнения и выводы)
billing_transactions (
    id UUID PRIMARY KEY,
    user_id BIGINT REFERENCES users,
    type VARCHAR NOT NULL,  -- topup | withdrawal
    amount DECIMAL(10,2) NOT NULL,
    stars_amount INTEGER,  -- кол-во Telegram Stars
    status VARCHAR DEFAULT 'pending',  -- pending | completed | failed | refunded
    provider_tx_id VARCHAR,  -- ID транзакции Telegram
    idempotency_key VARCHAR UNIQUE,  -- защита от дубликатов
    created_at TIMESTAMPTZ DEFAULT NOW(),
    completed_at TIMESTAMPTZ
)

-- Лог AI-запросов (только списания за AI)
ai_usage_log (
    id UUID PRIMARY KEY,
    user_id BIGINT REFERENCES users,
    query_type VARCHAR NOT NULL,  -- insight | portfolio | trade_advice | digest | chat
    cost DECIMAL(6,2),
    tokens_used INTEGER,
    created_at TIMESTAMPTZ DEFAULT NOW()
)

-- Кеш новостей (TTL: 30 дней, cleanup cron ежедневно)
news_cache (
    id UUID PRIMARY KEY,
    source VARCHAR NOT NULL,
    title TEXT NOT NULL,
    url TEXT,
    published_at TIMESTAMPTZ,
    summary TEXT,
    affected_tickers TEXT[],
    sentiment VARCHAR,  -- positive | negative | neutral
    expires_at TIMESTAMPTZ DEFAULT NOW() + INTERVAL '30 days'
)

-- Торговые заявки
trade_orders (
    id UUID PRIMARY KEY,
    user_id BIGINT REFERENCES users,
    ticker VARCHAR NOT NULL,
    direction VARCHAR NOT NULL,  -- buy | sell
    order_type VARCHAR DEFAULT 'market',  -- market | limit
    quantity INTEGER NOT NULL,
    price DECIMAL(12,4),
    status VARCHAR DEFAULT 'pending_confirmation',
    -- pending_confirmation | submitted | executed | cancelled | failed
    tbank_order_id VARCHAR,
    confirmed_at TIMESTAMPTZ,
    executed_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW()
)
```

### Ключевые индексы

```sql
CREATE INDEX idx_transactions_user_date ON transactions(user_id, date DESC);
CREATE INDEX idx_transactions_user_category ON transactions(user_id, category_id);
CREATE INDEX idx_portfolio_positions_user ON portfolio_positions(user_id);
CREATE INDEX idx_portfolio_operations_user ON portfolio_operations(user_id, executed_at DESC);
CREATE INDEX idx_trade_orders_user_status ON trade_orders(user_id, status);
CREATE INDEX idx_ai_usage_log_user ON ai_usage_log(user_id, created_at DESC);
CREATE INDEX idx_billing_transactions_user ON billing_transactions(user_id, created_at DESC);
CREATE INDEX idx_news_cache_published ON news_cache(published_at DESC);
CREATE INDEX idx_news_cache_expires ON news_cache(expires_at);
```

### Конкурентность AI-баланса

Списание AI-баланса использует optimistic locking:
```sql
UPDATE users
SET ai_balance = ai_balance - :cost, ai_balance_version = ai_balance_version + 1
WHERE telegram_id = :user_id
  AND ai_balance_version = :expected_version
  AND ai_balance >= :cost;
```
Если affected_rows = 0 → конфликт или недостаточно средств → повтор чтения.

## 7.5. Dashboard API

### Авторизация

JWT-токен, выдаётся при успешной проверке Telegram Login Widget hash. Передаётся в заголовке `Authorization: Bearer <token>`. Время жизни — 24 часа. CORS: разрешён только домен дашборда.

### Эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| POST | /api/auth/telegram | Проверка Telegram Login Widget, выдача JWT |
| GET | /api/me | Профиль пользователя |
| GET | /api/accounts | Список счетов и балансов |
| GET | /api/transactions | Транзакции (фильтры: period, category, account, search, group_by) |
| PATCH | /api/transactions/batch | Массовое обновление категорий |
| GET | /api/analytics/expenses | Расходы по категориям за период |
| GET | /api/analytics/cashflow | Доходы vs расходы по месяцам |
| GET | /api/portfolio | Текущий портфель |
| GET | /api/portfolio/history | История стоимости портфеля |
| GET | /api/portfolio/operations | Операции (покупки, дивиденды) |
| GET | /api/goals | Список целей |
| POST | /api/goals | Создание цели |
| PATCH | /api/goals/:id | Обновление цели |
| GET | /api/news | Персонализированная лента новостей |
| GET | /api/billing/balance | Текущий AI-баланс |
| GET | /api/billing/history | История операций с балансом |
| GET | /api/settings | Настройки пользователя |
| PATCH | /api/settings | Обновление настроек |
| GET | /api/integrations | Статус интеграций |
| POST | /api/integrations/:type/connect | Подключение интеграции |
| DELETE | /api/integrations/:type | Отключение интеграции |
| GET | /api/categories | Список категорий |
| POST | /api/categories | Создание категории |

Данные для дашборда обновляются через REST polling (интервал 60 секунд для портфеля, 5 минут для транзакций). WebSocket не требуется для MVP.

## 8. Безопасность

- **Токены интеграций:** шифрование AES-256, ключ в env-переменных
- **Торговля:** двойное подтверждение + настраиваемый лимит на сделку
- **AI-баланс:** нельзя уйти в минус; при выводе средств баланс уменьшается
- **Rate limiting:** 30 сообщений/минута на пользователя
- **Telegram webhook:** валидация подписи, HTTPS only
- **Дашборд auth:** Telegram Login Widget с проверкой hash по bot token
- **Дисклеймер:** при каждой торговой операции: "Сделка инициирована вами. Сервис не даёт инвестиционных рекомендаций"

### Персональные данные (152-ФЗ)

- **Уведомление Роскомнадзора** — до запуска подать уведомление об обработке ПД
- **Согласие на обработку** — при онбординге: явное согласие на обработку ПД (имя, telegram_id, финансовые данные)
- **Согласие на трансграничную передачу** — отдельное согласие на передачу анонимизированных данных в Claude API (Anthropic, США)
- **Локализация БД** — PostgreSQL на серверах в РФ
- **Анонимизация для AI** — перед отправкой в Claude API: удаление telegram_id, имени, номеров счетов. Только агрегированные суммы и тикеры
- **Право на удаление** — команда `/delete_account` полностью удаляет все данные пользователя в течение 30 дней
- **Политика конфиденциальности** — доступна по ссылке из бота и дашборда

## 9. Инфраструктура

### Локальная разработка

Docker Compose: app, ai-worker, cron-worker, trade-worker, postgres, redis.

### Продакшен

Kubernetes + ArgoCD. Каждый компонент — отдельный deployment/pod.

### Мониторинг (MVP)

- Sentry для ошибок
- Healthcheck endpoint
- Структурированные JSON-логи

### Бэкапы

- pg_dump ежедневно в S3-совместимое хранилище

## 10. Пополнение и вывод AI-баланса

### Пополнение

- **Способ:** Telegram Stars (нативная оплата в боте)
- **Минимум:** 100₽
- **Баланс бессрочный** (не сгорает)
- **Автопополнение:** опционально, при балансе ниже X₽

### Вывод

- **Механизм:** возврат Telegram Stars через Telegram Bot Payments API (refund). Средства возвращаются в Stars на аккаунт пользователя
- **Доступная сумма** = сумма пополнений (completed) - сумма списаний (ai_usage_log) - сумма предыдущих выводов (completed)
- **Минимальный вывод:** 50₽
- **При выводе** AI-баланс уменьшается. Баланс = 0 → платные функции недоступны
- **Фиксация:** все операции в `billing_transactions` (type: withdrawal, привязка к оригинальному topup через provider_tx_id)
- **Защита от злоупотреблений:** нельзя вывести больше, чем доступный баланс. Баланс рассчитывается из `billing_transactions` + `ai_usage_log`, а не из поля `users.ai_balance` (поле — кеш для быстрого доступа)

### Команды

```
/balance → текущий баланс + кнопки пополнения [+100₽] [+300₽] [+500₽]
/ai_history → история списаний и пополнений
/withdraw → вывод средств
```

## 11. Порядок реализации

| # | План | Содержание | Зависимости |
|---|------|-----------|-------------|
| 1 | Foundation | Структура, модели, миграции, Docker, auth | — |
| 2 | Telegram Bot Core | Команды, меню, ручной ввод расходов | Plan 1 |
| 3 | Integrations | ZenMoney + T-Bank Invest (read-only) | Plan 1 |
| 4 | Billing | Telegram Stars, баланс, пополнение, вывод | Plan 2 |
| 5 | AI System | Claude API, аналитика, дайджесты | Plans 3, 4 |
| 6 | Trading | Trade Worker, торговля через бот | Plans 3, 5 |
| 7 | News & Notifications | Парсинг новостей, персонализация, алерты | Plan 5 |
| 8 | Web Dashboard | React SPA, авторизация, все страницы | Plan 5 |
| 9 | K8s Deployment | Kubernetes manifests, ArgoCD, CI/CD | Plan 1 |

**MVP (первые пользователи):** Plans 1-5. Торговля, новости и дашборд — после валидации.

## 12. Внешние зависимости (оформить до начала Plan 2)

| Зависимость | Нужна для | Время получения | Статус |
|-------------|-----------|-----------------|--------|
| Telegram Bot Token (@BotFather) | Plan 2 | 5 минут | ❌ |
| Telegram Payments верификация | Plan 4 | дни-недели | ❌ |
| ZenMoney API ключи (consumer_key + secret) | Plan 3 | дни-недели | ❌ |
| T-Bank Invest API (sandbox) | Plan 3 | 1-2 дня | ❌ |
| Anthropic API Key | Plan 5 | 1 день | ❌ |
| Домен + SSL (для webhook) | Plan 2 | 1 день | ❌ |
| Согласование использования T-Bank API | Plan 6 | недели | ❌ |
| Уведомление Роскомнадзора (152-ФЗ) | Запуск | недели | ❌ |

## 13. Метрики успеха

### Activation
- % пользователей, подключивших интеграцию (цель: >50%)
- D1 retention — вернулись на второй день (цель: >40%)

### Engagement
- Среднее кол-во сообщений боту в день
- % пользователей, использующих AI-функции (цель: >20%)

### Revenue
- Конверсия в платных пользователей (цель: >5%)
- ARPU (средний доход на пользователя в месяц)
- Маржинальность AI-запросов (цена / себестоимость Claude API)

### Health
- Latency ответа бота (p50 < 1с, p95 < 3с)
- % неудачных синхронизаций
- AI Worker queue depth
